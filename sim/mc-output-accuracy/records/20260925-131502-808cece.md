# Record 20260925-131502-808cece

- **Record ID**: 20260925-131502-808cece
- **Experiment**: `mc-output-accuracy` — Output accuracy under device mismatch (Monte Carlo, sky130A tt_mm)
- **Claim**: Substantiates spec/target-spec.md's ratified "Output" row (1.8V +-2%, i.e. 1.764V-1.836V) against the LDO core-regulation-loop schematic landed by #14 and revised by #22 (current limit / soft start) and #25/#36 (current-mirror-OTA rail-to-rail output stage plus sized Miller compensation) (design/ldo_3v3in_1v8out.sch) under per-instance device mismatch, at a single representative operating point (nominal VIN=3.3V/27C/tt process corner, I_LOAD=1mA light load -- the same light-load convention sim/psrr-dc/ already uses). That row is ratified by issue #1 / DR-006, and the bound cited below is the ratified row's own window, not an invented final limit. This is a mismatch-only statistical check (klt sim's monte_carlo.vary="mismatch"): it re-runs this one PVT point N times with a fresh per-instance AGAUSS mismatch draw each time (sky130A's tt_mm .lib section -- see sim/README.md and docs/cli/sim.md in 2AMLogic/klayout-tools), NOT a re-run across the full PVT corner matrix (that is corner-run.py's separate job, e.g. sim/dropout-vs-load). Initial-condition contract (#164): the transient starts from the block's disabled state (all nodes 0, EN low) and powers up through its own soft-start ramp, the same enable-edge convention sim/startup uses -- it does NOT start from an unconstrained ngspice `.op`, which is not a physically realizable state of this block and which on 11/200 draws of this very sample sequence selected a spurious branch of the sky130 poly-resistor model's voltage-coefficient extrapolation instead of a circuit operating point (design/README.md "#164"). Known-remaining-gaps caveat (design/README.md "Known gaps / follow-on scope"): the #25/#36 revision closed the unsized-compensation and error-amplifier output-swing-ceiling gaps that dominated the #18-era records, but the pass device's own light-load pole and the absent Iq budget remain open, so a FAIL here is an honest finding about the current design state, not a harness defect.
- **Netlist provenance**: schematic (`sim/mc-output-accuracy/testbench/tb_mc_output_accuracy.sch`)
- **PDK**: sky130A @ open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b` (matches sim/pdk.json pin); models `/home/ubuntu/.volare/sky130A/libs.tech/combined/sky130.lib.spice`
- **Tools**: ngspice-46 : Circuit level simulation program; XSCHEM V3.4.7; klt 0.6.0+g7f53eec36f3a; Linux 7.0.0-1010-aws x86_64; klt sim backend `batch`
- **Repo state**: `808cece` on `feature/issue-164` (working tree dirty at run time)
- **Execution backend**: `batch` — samples executed by ngspice 42 as reported by `klt sim` **on the remote executor, not this machine**
  - Remote job: provider=`aws-batch-fleet`, job_id=`klt-sim-5dd2a249879f`, instance_type=`c7i.4xlarge`, lifecycle=`spot`, region=`us-east-1`, elapsed_seconds=`84`
  - The remote executor resolves its own PDK copy; `klt sim`'s own provenance block records it as `open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b` — compare that against the **PDK** line above, which is this machine's install.
- **Corner matrix run**:
  - Single mismatch-enabled point (not a PVT sweep — see Statistical convention below): process `tt_mm`, 27 °C, vsup default 3.3 V
  - **Not the full PVT matrix** — a Monte Carlo mismatch run samples device variation at one representative process/temperature/supply point per run, orthogonal to the PVT axis corner-run.py sweeps; see `sim/README.md`.
- **Statistical convention**: N=200 Monte Carlo samples, vary=`mismatch`, seed=`20260817` (klt sim's SHA-256-derived, reproducible seed contract — same seed reproduces the same sample sequence), k_sigma=3 (mean ± k·stddev limit-window check)
- **Result**:
  - klt sim aggregate status: **PASS** (200 pass / 0 fail / 0 error of 200 samples)
  - `vout_ss` (V, limits: min=1.764, max=1.836): **PASS**
    - n=200 (errored=0), mean=1.80157, stddev=0.00852292, min=1.77942, max=1.82499
    - quantiles: p5=1.78725, p50=1.80127, p95=1.81469
    - sigma_window (k=3): [1.776, 1.82714] — **PASS** (margin 0.00886318)
    - Nominal-vs-spread decomposition (computed by `mc-run.py` from the per-sample values in the klt response — see that function's docstring for how to read it):
      - in-window subpopulation: n=200 of 200 valued samples, mean=1.80157, stddev=0.00852292
      - nominal (centring) error: mean is +0.00156807 from the window centre 1.8 (+0.087 % of centre)
      - spread vs the window: the nearer window edge is 4.04σ from that subpopulation's mean
      - out-of-window samples: none
    - worst single sample: `tt_mm/novdd/27C/mc32` value=1.82499 margin=0.011013
  - Per-family mismatch-activity report (klt sim, from the netlist's own device instances):
    - `other`: unconfirmed — mismatch activity for the 'other' family is not independently verified for PDK family 'sky130'; treat any sampled spread for it as unconfirmed.
    - `capacitor`: unconfirmed — mismatch activity for the 'capacitor' family is not independently verified for PDK family 'sky130'; treat any sampled spread for it as unconfirmed.
    - `resistor`: unconfirmed — mismatch activity for the 'resistor' family is not independently verified for PDK family 'sky130'; treat any sampled spread for it as unconfirmed.
    - `mosfet`: ACTIVE — MOS per-instance mismatch is active in any _mm-suffixed process-corner section (e.g. tt_mm) -- select that section via corners.process to sample it (see docs/cli/sim.md's 'Known-good mismatch mechanisms').
  - **Overall: PASS**
- **Links**:
  - Testbench: `sim/mc-output-accuracy/testbench/tb_mc_output_accuracy.sch`
  - Netlist snapshot (klt-sim-ready): `sim/mc-output-accuracy/netlist-snapshots/20260925-131502-808cece.spice`
  - klt sim request: `sim/mc-output-accuracy/klt-requests/20260925-131502-808cece.json`
  - klt sim response (raw, full per-sample data): `sim/mc-output-accuracy/klt-responses/20260925-131502-808cece.json`
  - Machine-readable record: `sim/mc-output-accuracy/records/20260925-131502-808cece.json`
  - Experiment manifest: `sim/mc-output-accuracy/experiment.json`
- **Timestamp / author**: 2026-09-25T13:15:02Z, auditor@loom.local
- **Supersedes**: 20260925-110312-8280915

Written by `sim/bin/mc-run.py`. Append-only: never edit this file — a correction is a new record with a `Supersedes` field (see `sim/README.md`).
