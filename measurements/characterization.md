# LDO characterization report (DRAFT spec)

Generated by [`measurements/build_characterization_report.py`](build_characterization_report.py) — the capstone rollup of the block's own append-only evidence (DRC #16, LVS #17, full PVT-corner + Monte Carlo/yield #19, post-layout extracted-netlist #20), keyed to each row of [`spec/target-spec.md`](../spec/target-spec.md).

**This is not a final compliance claim.** `spec/target-spec.md` is still DRAFT — nothing in it is ratified (issue #1 open). Every verdict below is stated explicitly as *against the current DRAFT row*, per the same guardrail issue #19's own records already use. A `PASS`/`FAIL`/`MATCH`/`clean` value here is read verbatim from the cited record's own stated result — this script never recomputes a verdict from raw measurements (see the module docstring in `build_characterization_report.py` for the full extraction/freshness/no-re-derivation rules).

This file is a **generated rollup**, not itself append-only evidence in the `sim/README.md` sense: it is overwritten in place each time the generator runs, and git history — not a per-run record id — is its audit trail. Regenerate it (and commit the result) whenever the evidence it cites changes:

```bash
python3 measurements/build_characterization_report.py --out measurements/characterization.md
# or verify the committed file is not stale:
python3 measurements/build_characterization_report.py --check
```

Re-running this generator against an unchanged tree with the same pinned toolchain (`sim/pdk.json`) reproduces this file byte-for-byte — no wall-clock timestamps and no generating-commit identity are embedded, so `--check` passes both before and after the commit that lands a regenerated report. The per-`sim/` **Freshness** column below is a live check (a fresh `xschem` re-netlist of the current testbench schematic, compared verbatim against the committed netlist snapshot); on a machine without the PDK toolchain it degrades to `unverified` rather than a false claim of freshness (`--no-netlist-freshness` forces this explicitly). The layout (DRC/LVS/PEX) **Freshness** column instead compares the schematic/layout commit each record itself cites against the current git history / `LATEST*` pointers — no toolchain required.

No generating-commit SHA is stamped into this file, deliberately: a commit that regenerates this report cannot contain its own resulting hash, so such a line would make `--check` fail by construction on the very commit that ships the regenerated file. Provenance instead comes from the record ids cited per row (each of which is itself an append-only, commit-pinned record) plus this file's own git history.

## Per-spec-row characterization

| Parameter | DRAFT target (starting point) | Verdict (vs DRAFT) | Evidence | Freshness |
|---|---|---|---|---|
| Input | 3.3 V ±10% (2.97–3.63 V) | N/A | — | — |
| Output | 1.8 V ±2% (fixed; divider as a unit-resistor string) | **FAIL** | [`20260818-032827-81dc232`](../sim/mc-output-accuracy/records/20260818-032827-81dc232.md) | fresh |
| Load | 0–50 mA (0 mA = no external load; feedback divider is the only inherent preload) | N/A | — | — |
| Dropout @ 50 mA | < 300 mV | **FAIL** | [`20260818-032811-81dc232`](../sim/dropout-vs-load/records/20260818-032811-81dc232.md) | fresh |
| Line regulation | < 5 mV/V over 2.97–3.63 V, at 1 mA and 50 mA | **FAIL** (PVT subset) | [`20260825-040532-6fac47d`](../sim/line-regulation/records/20260825-040532-6fac47d.md) | fresh |
| Load regulation (0–50 mA) | < 1% (18 mV), counted inside the ±2% window | **FAIL** (PVT subset) | [`20260825-040748-6fac47d`](../sim/load-regulation/records/20260825-040748-6fac47d.md) | fresh |
| Load transient | 1↔50 mA step, ~1 µs edges: peak excursion ≤ 150 mV, recover to ±1% in ≤ 20 µs, over the ratified C_out/ESR window | **FAIL** | [`20260818-032755-81dc232`](../sim/load-transient/records/20260818-032755-81dc232.md) | fresh |
| PSRR | > 50 dB @ 1 kHz and > 20 dB @ 100 kHz, at 1 mA (light-load, binding) and at 50 mA | **FAIL** | [`20260818-032803-81dc232`](../sim/psrr-dc/records/20260818-032803-81dc232.md) | fresh |
| Iq (excl. load current) | < 30 µA at no load and full load | **PASS** (PVT subset) | [`20260825-040934-6fac47d`](../sim/iq/records/20260825-040934-6fac47d.md) | fresh |
| Current limit | constant-current (brickwall) clamp, window TBD over PVT; never engages for I_load ≤ 50 mA; survives continuous Vout = 0 short at Vin_max | **FAIL** (PVT subset) | [`20260825-070327-d0bb614`](../sim/current-limit/records/20260825-070327-d0bb614.md) | fresh |
| Startup / soft-start | monotonic into any load 0–50 mA and any C_out in the stability window; controlled ramp; inside ±2% within a few ms of enable; overshoot ≤ +2% | **PASS** (PVT subset) | [`20260825-044139-703a889`](../sim/startup/records/20260825-044139-703a889.md) | fresh |
| Enable / shutdown | shutdown Iq < 3 µA worst corner; disabled output = pass device fully off, no active discharge; Vin→Vout leakage ≤ 1 µA | **PASS** (PVT subset) | [`20260825-044140-703a889`](../sim/enable-shutdown/records/20260825-044140-703a889.md) | fresh |
| Thermal | continuous worst-case dissipation set by Vin_max × I_load; short-circuit ceiling set by the current limit; specified to Tj ≤ 125 °C; θJA delegated to package/integration | N/A | — | — |
| Output noise | not specified — waived unless a consumer states a requirement | N/A | — | — |
| Area | < 0.1 mm² total core area, pass FET included, excluding pads and sealring | N/A | — | — |
| Stability | stable 0–50 mA over the ratified C_out/ESR window; PM ≥ 45°, GM ≥ 10 dB worst corner | **FAIL** | [`20260818-032819-81dc232`](../sim/loop-gain/records/20260818-032819-81dc232.md) | fresh |

### Evidence detail

- **Input**: N/A — exercised as a stimulus condition (VIN step/sweep) inside every PVT testbench below, not measured by a testbench of its own.
- **Output**: **FAIL** (vs the current DRAFT spec row) — `sim/mc-output-accuracy` record [`20260818-032827-81dc232`](../sim/mc-output-accuracy/records/20260818-032827-81dc232.md), 181/200 individual sample(s) PASS. Freshness: fresh (a live xschem re-netlist of the current testbench schematic matches the committed netlist snapshot verbatim).
- **Load**: N/A — exercised as a stimulus condition (I_LOAD step/sweep) inside every PVT testbench below, not measured by a testbench of its own.
- **Dropout @ 50 mA**: **FAIL** (vs the current DRAFT spec row) — `sim/dropout-vs-load` record [`20260818-032811-81dc232`](../sim/dropout-vs-load/records/20260818-032811-81dc232.md), 0/45 corner(s) PASS. Freshness: fresh (a live xschem re-netlist of the current testbench schematic matches the committed netlist snapshot verbatim).
- **Line regulation**: **FAIL** (vs the current DRAFT spec row) — `sim/line-regulation` record [`20260825-040532-6fac47d`](../sim/line-regulation/records/20260825-040532-6fac47d.md), 2/3 corner(s) PASS. Freshness: fresh (a live xschem re-netlist of the current testbench schematic matches the committed netlist snapshot verbatim). **PVT subset, not the full matrix this experiment declares** — the record's own stated reason: harness bring-up (issue #64) -- 3-corner quick subset, matching issue #18's original precedent for newly-shipped testbenches; full 45-point matrix is follow-on work per that same precedent
- **Load regulation (0–50 mA)**: **FAIL** (vs the current DRAFT spec row) — `sim/load-regulation` record [`20260825-040748-6fac47d`](../sim/load-regulation/records/20260825-040748-6fac47d.md), 2/3 corner(s) PASS. Freshness: fresh (a live xschem re-netlist of the current testbench schematic matches the committed netlist snapshot verbatim). **PVT subset, not the full matrix this experiment declares** — the record's own stated reason: harness bring-up (issue #64) -- 3-corner quick subset, matching issue #18's original precedent for newly-shipped testbenches; full 45-point matrix is follow-on work per that same precedent
- **Load transient**: **FAIL** (vs the current DRAFT spec row) — `sim/load-transient` record [`20260818-032755-81dc232`](../sim/load-transient/records/20260818-032755-81dc232.md), 23/45 corner(s) PASS. Freshness: fresh (a live xschem re-netlist of the current testbench schematic matches the committed netlist snapshot verbatim).
- **PSRR**: **FAIL** (vs the current DRAFT spec row) — `sim/psrr-dc` record [`20260818-032803-81dc232`](../sim/psrr-dc/records/20260818-032803-81dc232.md), 6/45 corner(s) PASS. Freshness: fresh (a live xschem re-netlist of the current testbench schematic matches the committed netlist snapshot verbatim).
- **Iq (excl. load current)**: **PASS** (vs the current DRAFT spec row) — `sim/iq` record [`20260825-040934-6fac47d`](../sim/iq/records/20260825-040934-6fac47d.md), 3/3 corner(s) PASS. Freshness: fresh (a live xschem re-netlist of the current testbench schematic matches the committed netlist snapshot verbatim). **PVT subset, not the full matrix this experiment declares** — the record's own stated reason: harness bring-up (issue #64) -- 3-corner quick subset, matching issue #18's original precedent for newly-shipped testbenches; full 45-point matrix is follow-on work per that same precedent
- **Current limit**: **FAIL** (vs the current DRAFT spec row) — `sim/current-limit` record [`20260825-070327-d0bb614`](../sim/current-limit/records/20260825-070327-d0bb614.md), 2/3 corner(s) PASS. Freshness: fresh (a live xschem re-netlist of the current testbench schematic matches the committed netlist snapshot verbatim). **PVT subset, not the full matrix this experiment declares** — the record's own stated reason: Issue #65 first record for this testbench: 3-point PVT subset -- tt/27C/3.30V (nominal), ss/-40C/2.97V (slow, cold, low supply) and ff/125C/3.63V (fast, hot, high supply), the same subset every other LDO experiment here uses to stand a new bench up. The full 45-point matrix this manifest declares is tracked as its own unit of work in issue #74.
- **Startup / soft-start**: **PASS** (vs the current DRAFT spec row) — `sim/startup` record [`20260825-044139-703a889`](../sim/startup/records/20260825-044139-703a889.md), 3/3 corner(s) PASS. Freshness: fresh (a live xschem re-netlist of the current testbench schematic matches the committed netlist snapshot verbatim). **PVT subset, not the full matrix this experiment declares** — the record's own stated reason: Issue #65 first record for this testbench: 3-point PVT subset -- tt/27C/3.30V (nominal), ss/-40C/2.97V (slow, cold, low supply) and ff/125C/3.63V (fast, hot, high supply), the same subset every other LDO experiment here uses to stand a new bench up. The full 45-point matrix this manifest declares is tracked as its own unit of work in issue #74 (measured ~5 min/corner for this bench on this host), mirroring how issue #19 followed up the quick-subset records issues #18/#25 first minted for every other LDO testbench here.
- **Enable / shutdown**: **PASS** (vs the current DRAFT spec row) — `sim/enable-shutdown` record [`20260825-044140-703a889`](../sim/enable-shutdown/records/20260825-044140-703a889.md), 3/3 corner(s) PASS. Freshness: fresh (a live xschem re-netlist of the current testbench schematic matches the committed netlist snapshot verbatim). **PVT subset, not the full matrix this experiment declares** — the record's own stated reason: Issue #65 first record for this testbench: 3-point PVT subset -- tt/27C/3.30V (nominal), ss/-40C/2.97V (slow, cold, low supply) and ff/125C/3.63V (fast, hot, high supply), the same subset every other LDO experiment here uses to stand a new bench up. The full 45-point matrix this manifest declares is tracked as its own unit of work in issue #74 (measured ~70 s/corner for this bench on this host), mirroring how issue #19 followed up the quick-subset records issues #18/#25 first minted for every other LDO testbench here.
- **Thermal**: N/A — the thermal-shutdown circuitry (issue #35) is present in the schematic, but no dedicated thermal testbench exists yet under `sim/`.
- **Output noise**: N/A — waived by the spec row itself ("not specified — waived unless a consumer states a requirement").
- **Area**: N/A — no dedicated area-extraction check exists yet; the routed layout's own geometry is not re-derived into a verdict here, per this report's own no-re-derivation rule.
- **Stability**: **FAIL** (vs the current DRAFT spec row) — `sim/loop-gain` record [`20260818-032819-81dc232`](../sim/loop-gain/records/20260818-032819-81dc232.md), 3/45 corner(s) PASS. Freshness: fresh (a live xschem re-netlist of the current testbench schematic matches the committed netlist snapshot verbatim).

## Layout verification (not itself a spec row)

DRC/LVS/post-layout PEX substantiate that the routed layout matches the schematic these spec-row testbenches above simulate — they are not measurements of a numeric spec parameter themselves, so they are rolled up separately rather than forced into the table above.

| Check | Verdict (record's own) | Record | Freshness |
|---|---|---|---|
| DRC (issue #16) | **PASS** (status=clean, violation_count=0) | [`20260825-015012-3149121`](../layout/ldo-core/reports/20260825-015012-3149121/record.md) | fresh |
| LVS (issue #17) | **MATCH** (status=match, mismatch_count=3) | [`20260825-015029-3149121`](../layout/ldo-core/reports/20260825-015029-3149121/record.md) | fresh |
| Post-layout PEX (issue #20) | see detail — no single PASS/FAIL ([caveat](../sim/pex-post-layout/README.md)) | [`20260825-052509-6fac47d`](../sim/pex-post-layout/records/20260825-052509-6fac47d.md) | fresh |

Post-layout PEX detail: `klt sim` (schematic-side leg, standalone): status=pass, corners=45, passed=45, failed=0, errored=0; `klt pex` (schematic + extracted legs + delta): status=error, passed=0, failed=0, errored=135, pin_count_mismatch=None

## Limitations

- **DRAFT spec.** Every verdict above is against `spec/target-spec.md`'s current DRAFT row, not a ratified target. Re-verification is required once issue #1 (spec ratification) and the open decision records (`spec/decision-records/`) resolve.
- **N/A rows are a coverage gap, not a pass.** A row marked N/A above has no independent testbench yet — it is neither substantiated nor refuted by this report.
- **A `(PVT subset)` verdict is narrower than a full-matrix one.** That marker means the cited record ran fewer corners than its own `experiment.json` declares (the runner refuses to write such a record without a stated reason, quoted in the Evidence detail above). A subset `PASS` says the corners that ran passed — it does not say the full matrix would.
- **Freshness checks trust the tree, not the working copy.** The schematic-freshness check (layout section) compares against git history, so uncommitted local edits to `design/ldo_3v3in_1v8out.sch` will not be detected as stale until committed.
- **Post-layout PEX (issue #20) has no single PASS/FAIL** — see `sim/pex-post-layout/README.md` for the three disclosed, real upstream `klt`/PDK-model-interaction gaps that currently bound the extracted-side leg.

